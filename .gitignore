# Terraform files and directories
**/.terraform/          # Ignore Terraform provider binaries and external modules
*.tfstate               # Ignore Terraform state files
*.tfstate.*             # Ignore Terraform state backup files
*.tfvars                # Ignore variable files (may contain sensitive data)
*.tfvars.json           # Ignore JSON variable files
*.tfplan                # Ignore Terraform plan output files
crash.log               # Ignore crash logs generated by Terraform
.terraformrc            # Ignore Terraform CLI configuration files
terraform.rc            # Ignore additional CLI configuration files

# Ansible files and directories
*.retry                 # Ignore Ansible retry files
vault_pass.txt          # Ignore Ansible Vault password file (if used)
variables.yml           # Ignore sensitive variables file (can be encrypted using ansible-vault)
group_vars/*.yml        # Ignore group-specific variable files (if sensitive)
host_vars/*.yml         # Ignore host-specific variable files (if sensitive)
roles/*/files/**        # Ignore all files inside 'files' folder in roles
roles/*/templates/**    # Ignore all templates inside 'templates' folder in roles
hosts/*

# Environment variables
*.env                    # Ignore .env file containing environment variables

# General sensitive files
*.pem                   # Ignore private keys (e.g., SSH keys)
*.pfx                   # Ignore certificate files
credentials.txt         # Ignore credentials file

# System-specific or IDE-related files
.DS_Store               # macOS system file
Thumbs.db               # Windows system file
.idea/                  # IntelliJ IDEA project settings
.vscode/                # VS Code project settings

# Logs and temporary files
*.log                   # Log files
*.tmp                   # Temporary files

# Override rules (optional)
!example_override.tf    # Include specific override Terraform file if needed
